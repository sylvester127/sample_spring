<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.repository.UserMapper">
	<insert id = "insertUser" parameterType = "com.example.demo.domain.UserVO">
		INSERT	INTO
		USER	(USERID, PASSWORD, NAME, EMAIL, BIRTHDAY, ADDRESS, PHONE, SEX, U_REMV_FLAG)
		VALUES	(#{userId}, #{password}, #{name}, #{email}, #{birthday}, #{address}, #{phone}, #{sex}, 'N')
		
	</insert>
	
	<select id="loginUser" parameterType="com.example.demo.domain.UserVO" resultType="com.example.demo.domain.UserVO">
	SELECT 
		USERID
	 	,PASSWORD
	 FROM
	 	USER
	 WHERE
	 	USERID = #{userId} AND
	 	PASSWORD = #{password}

	</select>
	
	<select id="checkUser" resultType="com.example.demo.domain.UserVO">
	SELECT
		USERID,
		PASSWORD
	FROM
		USER
	WHERE
		USERID = #{id} AND
	 	PASSWORD = #{pw}
	</select>
</mapper>